import { FilterUserPipePipe } from './filter-user.pipe';

describe('FilterUserPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserPipePipe();
    expect(pipe).toBeTruthy();
  });
});
